import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TaskCardFooter = () => {
  return (
    <View style={{ marginTop: 6 }}>
      <Text>TaskCardFooter</Text>
    </View>
  );
};

export default TaskCardFooter;

const styles = StyleSheet.create({});
